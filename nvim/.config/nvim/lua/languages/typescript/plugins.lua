local M = {}

-- LSP-related plugins that need to be loaded as dependencies
M.lsp_plugins = {
	"yioneko/nvim-vtsls", -- For TypeScript LSP enhancements
}

-- Other TypeScript-specific plugins (testing, syntax, etc.)
M.plugins = {
	-- Add other TypeScript plugins here as needed
}

return M